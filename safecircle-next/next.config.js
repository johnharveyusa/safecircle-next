/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow fetching from ArcGIS and external sources client-side
  // (no server-side proxying needed for public ESRI endpoints)
}

module.exports = nextConfig
